import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  footer:{
    top: 'auto',
    bottom: 0,
    background: '#ffffff',
    padding: '10px 50px',
  },
  heading: {
    color: 'navy',
    textDecoration: 'none',
  },
  a: {
    color: 'navy'
  },
  divider: {
      background: 'navy'
  }
}));